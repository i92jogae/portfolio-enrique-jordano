---
const { class: className = "", id } = Astro.props;
---

<section id={id} class={`${className ?? ''} group/section w-full lg:max-w-5xl md:max-w-3xl scroll-m-24 mx-auto py-10 md:py-16`}>
  <slot />
</section>
